$ npm install -g typescript

$ npm install -g ts-node

$ npm install -g npm

$ npm -v

$ npm install -g @angular/cli

$ ng versión

$ ng new ejemploAngular --skip-tests

a todo no


después dentro del proyecto instalamos bootsrap

npm i Bootstrap

npm install sweetalert2


este es para iniciar el proyecto
$ ng s -o


luego abrimos otro terminal entramos en la ruta del proyecto y escribimos code .


		"styles": [
              "src/styles.css", "node_modules/bootstrap/dist/css/bootstrap.min.css"
            ], "scripts": ["node_modules/bootstrap/dist/js/bootstrap.min.js"]



ng g c components/component-navbar --skip-tests

ng g i interfaces/interface-noticia --type = interface

ng g s services/service-productos

ng g guard guards/guard

ng g interceptor interceptors/auth

esto fuera del proyecto
npm i -g firebase-tools

firebase login


apartir de aquí dentro del proyecto

//esto es para iniciarle
firebase init

le das a que si

escogemos hosting

public

no

no

//esto es para compilarlo
ng build --configuration=production


//modificar public en firebase.json para que sea accesible a el index.html


firebase deploy

